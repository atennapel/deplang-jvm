(<<) : {A B C : U0 Val} -> (B -> C) -> (A -> B) -> A -> C
  = \f g x. f (g x);

(+) : Nat -> Nat -> Nat := \a b. foldNat a b (\_ r. S r);
(*) : Nat -> Nat -> Nat := \a b. foldNat a 0 (\_ r. b + r);

m2a1 : Nat -> Nat := (<<) {Nat} {Nat} {Nat} ((+) 1) ((*) 2);
